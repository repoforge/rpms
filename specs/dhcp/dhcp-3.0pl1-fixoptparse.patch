--- dhcp-3.0pl1/client/dhclient.c.fixoptparse	2002-11-14 13:27:28.000000000 -0500
+++ dhcp-3.0pl1/client/dhclient.c	2002-11-14 13:27:46.000000000 -0500
@@ -198,8 +198,6 @@
 		} else if (!strcmp (argv [i], "-w")) {
 			/* do not exit if there are no broadcast interfaces. */
 			persist = 1;
- 		} else if (argv [i][0] == '-') {
- 		    usage ();
 		} else if (!strcmp (argv [i], "-e")) {
 			struct string_list *tmp;
 			if (++i == argc)
@@ -216,6 +214,8 @@
 			exit (0);
 		} else if (!strcmp (argv [i], "-nw")) {
 			nowait = 1;
+ 		} else if (argv [i][0] == '-') {
+ 		    usage ();
  		} else {
  		    struct interface_info *tmp = (struct interface_info *)0;
 		    status = interface_allocate (&tmp, MDL);
